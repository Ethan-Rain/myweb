<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.helloworld1999.myweb.mapper.ImageMetadataMapper">

    <resultMap id="BaseResultMap" type="cn.helloworld1999.myweb.demos.api.model.ImageMetadata">
            <id property="imageId" column="image_id" />
            <result property="rating" column="rating" />
            <result property="description" column="description" />
            <result property="uploader" column="uploader" />
            <result property="uploadTime" column="upload_time" />
    </resultMap>

    <sql id="Base_Column_List">
        image_id,rating,description,uploader,upload_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from image_metadata
        where  image_id = #{imageId} 
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from image_metadata
        where  image_id = #{imageId} 
    </delete>
    <insert id="insert" keyColumn="image_id" keyProperty="imageId" parameterType="cn.helloworld1999.myweb.demos.api.model.ImageMetadata" useGeneratedKeys="true">
        insert into image_metadata
        ( image_id,rating,description,uploader,upload_time)
        values (#{imageId},#{rating},#{description},#{uploader},#{uploadTime})
    </insert>
    <insert id="insertSelective" keyColumn="image_id" keyProperty="imageId" parameterType="cn.helloworld1999.myweb.demos.api.model.ImageMetadata" useGeneratedKeys="true">
        insert into image_metadata
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="imageId != null">image_id,</if>
                <if test="rating != null">rating,</if>
                <if test="description != null">description,</if>
                <if test="uploader != null">uploader,</if>
                <if test="uploadTime != null">upload_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="imageId != null">#{imageId},</if>
                <if test="rating != null">#{rating},</if>
                <if test="description != null">#{description},</if>
                <if test="uploader != null">#{uploader},</if>
                <if test="uploadTime != null">#{uploadTime},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="cn.helloworld1999.myweb.demos.api.model.ImageMetadata">
        update image_metadata
        <set>
                <if test="rating != null">
                    rating = #{rating},
                </if>
                <if test="description != null">
                    description = #{description},
                </if>
                <if test="uploader != null">
                    uploader = #{uploader},
                </if>
                <if test="uploadTime != null">
                    upload_time = #{uploadTime},
                </if>
        </set>
        where   image_id = #{imageId} 
    </update>
    <update id="updateByPrimaryKey" parameterType="cn.helloworld1999.myweb.demos.api.model.ImageMetadata">
        update image_metadata
        set 
            rating =  #{rating},
            description =  #{description},
            uploader =  #{uploader},
            upload_time =  #{uploadTime}
        where   image_id = #{imageId} 
    </update>
</mapper>
